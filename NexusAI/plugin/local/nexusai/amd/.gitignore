# Source maps generated by Webpack — not committed.
build/*.map

# But the bundle IS committed (build/chatwidget-lazy.min.js)
# so the plugin works out of the box without needing to
# run npm install. This changes when the build moves to CI.
!build/chatwidget-lazy.min.js
